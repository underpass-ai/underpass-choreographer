{{- if .Values.persistence.ceremonies.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "choreographer.fullname" . }}-ceremonies
  labels:
    {{- include "choreographer.labels" . | nindent 4 }}
spec:
  accessModes:
    # ReadWriteOnce, not ReadWriteMany: the store serves one writer, and
    # an access mode that suggested otherwise would invite exactly the
    # topology it cannot support.
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.ceremonies.size | quote }}
  {{- with .Values.persistence.ceremonies.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end }}
